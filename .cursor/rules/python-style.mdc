---
description: Python 编码规范（Google 风格指南要点，中文）
alwaysApply: true
---

# Python 编码规范

## 分号与语句

- 行尾不要分号；不要用分号把两条语句写在同一行。
- 通常每个语句独占一行；仅当 `if` 无 `else` 且条件与体可安全挤在一行时可写 `if cond: body()`。`try`/`except` 不可挤在同一行。

## 行宽与续行

- 最大行宽 **80** 字符；长 import、注释里的 URL/路径、难拆分的模块级常量、`# pylint: disable=...` 可例外；Black/Pyink 无法拆分时允许超长。
- **禁止**用反斜杠做显式续行；用圆括号、方括号、花括号的隐式续行；必要时给表达式外包一层 `()`。
- 多行字符串字面量用括号隐式拼接： `('a' 'b')`。
- 换行优先在最外层语法结构；链式调用在 `.` 前断行；`if (` 多条件时每个条件可独立一行。
- 注释里的长 URL 单独成行，不要用 `\` 拆 URL。

## 括号与缩进

- `if`/`while`/`return` 等不要多余括号；单元素元组用 `(x,)`。
- **4 个空格**缩进，禁用 Tab；续行与左括号对齐，或 **4 空格悬挂缩进**（续行首行不放参数）；右括号可与末元素同行或与左括号列对齐。
- 多行 `]` `)` `}` 与最后元素不同行时，**推荐尾部逗号**（与格式化工具一致）。

## 文档字符串与注释

- 文档字符串必须用 **`"""`**（PEP 257）；首行一句概述（≤80 字符，句末标点），多段时概述后空一行再正文，正文与开引号列对齐。
- 公开 API、非平凡逻辑须有文档字符串；Google 式小节标题用中文时可写 **参数:**、**返回:**、**生成:**、**抛出:**（或 Args/Returns/Yields/Raises，**同一文件内统一**）；小节正文相对标题 **2 或 4 格悬挂缩进**（文件内统一）。不要 NumPy 式“多返回值像多个名字”的写法。
- 类文档描述实例“是什么”；`Exception` 子类描述异常含义，不要写“在某某情况下抛出”。
- 块注释解释意图；`#` 与代码间至少 **2 空格**，`#` 与注释内容至少 **1 空格**。不要复述代码在做什么。
- 标点、拼写、语法写清楚；短行尾注释可略随意但要一致。

## 字符串与日志

- 格式化用 **f-string**、`%` 或 `format()`；不要用 `+` 拼出格式化结果。单次 `a + b` 拼接可以。
- 循环内累积字符串用 **`''.join(list)`** 或 `io.StringIO`，避免 `+=` 链。
- 同一文件引号风格一致（`'` 或 `"`）；需转义时换另一种外层引号。多行非 docstring 优先 `"""`；docstring **必须** `"""`。
- 对标准 logging 风格 API：**第一个参数必须是带 `%` 占位符的字面量**，变量用后续参数传入，**不要用 f-string 当第一个参数**。

## 错误信息

- 精确对应真实错误条件；插入片段可区分；便于 grep/自动化处理。例如概率校验用 `not 0 <= p <= 1` 而非分开比较（避免 NaN 漏网）。

## 资源与 TODO

- 文件、套接字、DB 等有状态资源用 **`with`**；无 `with` 协议时用 `contextlib.closing()`。不要依赖 `__del__` 关资源。
- TODO 格式：`# TODO(上下文): 说明`，上下文优先 issue 链接；避免模糊“以后改”。

## 导入

- 一般 **一行一个 import**；**例外**：`from collections.abc import A, B` 与 `from typing import ...` 可多符号一行。
- 顺序：`__future__` → 标准库 → 第三方 → 本仓库包；组内按模块路径**字典序**（忽略大小写）；组间可空行。
- 类型专用导入可放 `if TYPE_CHECKING:`（紧随普通 import，块内不空行，注解中按需字符串引用）。

## 命名（摘要）

| 类别     | 公有           | 内部 / 受保护   |
|----------|----------------|-----------------|
| 模块/包  | `lower_with_under` | `_lower_with_under` |
| 类/异常  | `CapWords`     | `_CapWords`（类） |
| 函数/方法 | `lower_with_under()` | `_lower_with_under()` |
| 常量     | `CAPS_WITH_UNDER` | `_CAPS_WITH_UNDER` |
| 变量/参数/属性 | `lower_with_under` | `_` 前缀表示内部或受保护 |

- 文件名 **`*.py`**，名中无连字符。单字母名仅用于 `i,j,k`、异常 `e`、`with` 文件句柄 `f`、私有无约束 TypeVar 等批准场景。
- 双下划线 name mangling（`__foo`）**不鼓励**；内部优先单下划线。

## 主入口与函数体量

- 可执行脚本：主要逻辑放在 `main()`，并用 `if __name__ == '__main__':` 调用；避免 import 时执行副作用。
- 函数宜短而专；超过约 **40 行**考虑拆分（非硬性，但应权衡）。

## 类型注解（摘要）

- 熟读 PEP 484；公开 API、易错处、难懂处优先注解；非必要不注解 `self`/`cls`/`__init__` 返回。
- 并集推荐 **`X | None`**；避免隐式 `a: str = None` 表示 Optional。
- 仅带注解的默认值参数：`def f(a: int = 0)` 中 **`=` 两侧有空格**；`a:int=0` 禁止。
- 长签名每参数一行，末参数可加逗号再 `) -> R:`；尽量不在“变量名与类型”之间断行；别名用 `CapWords` 或 `_Private`（模块私有）。
- 泛型尽量写全类型参数；需要 `Any` 时显式写。`TypeVar`/`ParamSpec` 对外不可见且无约束可用 `_T`、`_P`。
- 用 `str`/`bytes`，不用 `typing.Text`（新代码）。`from typing` / `collections.abc` **导入具体符号**用于注解；优先 PEP 585 内置泛型（`list[str]` 等），除非兼容旧库（如 Beam）。

## 反例速记

```python
# 不好：显式续行、行尾分号
if x and y and \
    z:
    pass;

# 好：括号续行
if (
    x
    and y
    and z
):
    pass
```

```python
# 不好：logging 用 f-string 首参
logger.info(f"value={x}")

# 好
logger.info("value=%s", x)
```
